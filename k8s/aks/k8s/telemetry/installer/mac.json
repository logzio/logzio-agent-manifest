{
    "commands": [
        {
            "description": "Create set for taints",
            "run": [
                "params=$(jq -r .subtype.datasources[0].params $app_json);",
                "while read line; do",
                    "echo test;",
                "done <<<",
                "\"$(echo $params | jq -c .[])\"",
                ";"
            ]
        }
    ]
}