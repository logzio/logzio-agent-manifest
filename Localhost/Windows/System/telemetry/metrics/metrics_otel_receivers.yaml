receivers:
  hostmetrics:
    collection_interval: 30s
    scrapers:
      cpu:
      memory:
      disk:
      load:
      filesystem:
      network:
      paging:
      process:
        mute_process_name_error: true
