receivers:
processors:
  resourcedetection/system:
    detectors: ["system"]
    system:
      hostname_sources: ["os"]
exporters:
  logging:
service:
  pipelines:
    logs:
      receivers: []
      processors: [resourcedetection/system]
      exporters: []
    metrics:
      receivers: []
      processors: [resourcedetection/system]
      exporters: []
  telemetry:
    logs:
      level: "debug"
